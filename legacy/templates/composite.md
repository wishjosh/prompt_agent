<!--
====================================================================
 Composite 코어 템플릿 (발판형, medium 균형선)
 - 이 파일을 복사해 새 프롬프트를 시작한다. 이 주석 블록은 지우고 쓴다.
 - [필수] 4섹션(메타·목표·역할·제약)은 비우지 마라. 못 채우면 사고가 덜 된 신호다.
 - [선택] 섹션은 불필요하면 "통째로 삭제"하라. 빈 제목만 남기지 마라.
 - 환경(NotebookLM·Claude Code 등) 언급은 절대 코어에 넣지 마라 → 어댑터로.
 - 구조 기준: ../../../meta/CORE_ADAPTER_ARCHITECTURE.md
====================================================================
-->

# {프롬프트명} — {한 줄 설명}

> **먼저 물어라**: 이 프롬프트는 연구자의 *어떤 판단*의 비용을 낮추는가? 한 문장으로 안 나오면, 아직 만들 때가 아니다.
> **코어 파일 (환경 중립)**: 실행하려면 환경 어댑터와 함께 사용한다. 짝 어댑터: `adapters/{ID}.{환경}.md`

## [필수] 메타 헤더

| 항목 | 값 |
|------|-----|
| ID | {예: RES-00X} |
| 층 | Composite |
| 매체 | {영상 / 논문 / 법령 / 기사 … 또는 "매체 무관"} |
| AI 역할 | {Facilitator / Critical Friend / 전환} |
| 도메인 | {research / personal …} |
| 버전 | v1.0 |
| 계보 | {전신 프롬프트가 있으면 한 줄. 없으면 이 행 삭제} |

## [필수] 목표 행위

{입력 → 출력을 1~2문장으로. 그리고 "어떤 판단의 비용을 낮추는가"를 명시한다.}

## [필수] AI 역할

- **모드**: {Facilitator(조력) / Critical Friend(비판) / 국면별 전환}
- {전환 모드일 때만} **전환 트리거**: {어떤 조건에서 역할이 바뀌는가}

## [필수] 제약조건 (행위 원칙 — 최소 1개)

> 환경 어휘("선택된 소스", "인용 마커" 등)는 여기 넣지 마라 → 어댑터 소관.
> 출처 표기가 필요하면 "출력의 모든 항목에 출처 토큰을 채워라"처럼 *행위*로만 적고, 형식은 어댑터에 맡긴다.

1. {예: 환각 금지 — 대상 소스 본문 밖 사실을 끌어들이지 마라}
2. {예: 가치 판단 배제 / 자의적 재구성 금지 — 해당할 때만}

---
<!-- 여기부터는 [선택] 섹션. 불필요하면 섹션째 삭제. -->

## [선택] 위임 가능 범위

{가용성(위임받을 마찰) / 주도권(연구자가 쥔 판단). 목표 행위에 이미 드러나면 이 섹션 삭제.}

## [선택] 모드 정의

{deep/brief/recipe 처럼 입력 유형에 따라 분기가 필요할 때만. 단일 행위면 삭제.}

## [선택] 절차

> ⚠️ **선형 강제 아님** (원칙 2): 순서가 의미 있으면 Step으로, 순서가 무의미하면 "꺼내 쓰는 도구 목록"으로, 입력 유형별로 갈리면 "모드 분기"로 쓴다. 절차가 자명하면 이 섹션을 비워도 된다.

### Step 1. {단계명}
{지시}

## [선택] 출력 형식

> **출처 토큰 규약**: 출처가 필요한 자리에 `[출처]` / `[시점]` / `[출처+시점]` 토큰을 둔다. 실제 마커 형식은 어댑터가 치환한다.
> 구조화된 출력은 세렌디피티의 조건이다(원칙 4). 다만 산문 응답이 더 맞으면 템플릿 대신 "출력의 성격" 1줄로 대체해도 된다.

```markdown
{기대 출력 구조. 항목 끝에 출처 토큰 부착.}
```

## [선택] 입력 변수 명세

> 전달 방식(사용자에게 어떻게 보여주고 채우게 할지)은 어댑터가 정의한다. 여기서는 슬롯만 선언.

```
INPUTS:
- {slot_name}   (선택/필수) {설명}
```

---

버전: v1.0 | 상태: draft | 최종 수정: {YYYY-MM-DD}
