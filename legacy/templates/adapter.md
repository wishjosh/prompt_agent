<!--
====================================================================
 Adapter 템플릿 (환경별 실행 인터페이스)
 - 코어가 비워둔 "환경 부분"만 채운다. 행위 정의는 코어에 있고, 여기 중복하지 않는다.
 - 환경마다 같은 항목을 채워야 비교가 쉬우므로 구조는 비교적 고정(tight).
 - 해당 없는 섹션은 삭제하지 말고 "없음"으로 명시한다(환경 간 비교 위해).
 - 파일명: {코어ID}.{환경슬러그}.md  (예: RES-002-G.notebooklm.md)
 - 이 주석 블록은 지우고 쓴다. 구조 기준: ../../../meta/CORE_ADAPTER_ARCHITECTURE.md
====================================================================
-->

# {코어ID} × {환경} 어댑터

> **어댑터 파일**: 코어 `{코어파일명}`와 함께 사용한다.
> **조립 순서**: 코어 본문 전체 → 가로줄(`---`) → 본 어댑터 본문 순으로 대화창에 붙여넣고, 맨 아래 입력란을 채워 실행한다.

| 항목 | 값 |
|------|-----|
| 짝 코어 | {ID} |
| 환경 | {NotebookLM / Claude.ai / Claude Code / Codex / Gemini / Antigravity} |
| 버전 | v1.0 |

## 1. 실행 전 준비

{이 환경에서 대상 소스를 어떻게 투입하는가. 예: 체크박스 선택 / 파일 첨부 / 파일 경로 지정}

## 2. 출처 토큰 치환 규칙 (횡단)

코어 출력 형식의 토큰을 다음 형식으로 치환하라. 모든 토큰을 빠짐없이 채운다.

| 코어 토큰 | {환경} 출력 형식 | 예시 |
|---|---|---|
| `[출처]` | {} | {} |
| `[시점]` | {해당 없으면 "없음"} | {} |
| `[출처+시점]` | {} | {} |

## 3. 환경 특화 제약

{이 환경에서만 필요한 추가 제약. 없으면 "없음".}

## 4. 입력 전달

> 코어가 선언한 입력 슬롯(INPUTS)을 사용자에게 어떻게 보여주고 채우게 할지.

{예: "아래를 복사한 후 괄호를 채워 코어·어댑터 뒤에 이어 실행하세요." + 슬롯별 안내}

---

버전: v1.0 | 상태: draft | 최종 수정: {YYYY-MM-DD}
