# res_digest_video_deep × NotebookLM 어댑터

> **어댑터 파일**: 코어 [`res_digest_video_deep.md`](../core/res_digest_video_deep.md)와 함께 사용한다.
> **조립 순서**: 코어 본문 전체 → 가로줄(`---`) → 본 어댑터 본문 순으로 대화창에 붙여넣고, 맨 아래 입력란을 채워 실행한다.
> 자세한 규약: [meta/CORE_ADAPTER_ARCHITECTURE.md](../../../../meta/CORE_ADAPTER_ARCHITECTURE.md)

| 항목 | 값 |
|------|-----|
| 짝 코어 | res_digest_video_deep |
| 환경 | NotebookLM (notebooklm.google.com) |
| 버전 | v1.0 |

## 1. 실행 전 준비 (NotebookLM)

1. `res_digest_video_route`로 **`deep` 추천을 받은 영상 1개만** 체크박스에서 '선택'한 상태로 실행하세요. (여러 개 선택 시 구조가 섞입니다)
2. brief/recipe로 추천된 영상이면 해당 모드 프롬프트를 사용하세요.
3. 코어 + 본 어댑터를 함께 복사해 **NotebookLM Studio의 '보고서 기능'에 붙여넣어 실행**하세요. (단일 소스 해체는 채팅창보다 Studio 보고서 기능이 결과가 안정적 — 2026-05 실사용 확인.)

## 2. 출처 플레이스홀더 치환 규칙 (횡단)

코어 출력 형식의 환경 중립 토큰을 다음 NotebookLM 형식으로 치환하여 출력하라:

| 코어 토큰 | NotebookLM 출력 형식 | 예시 |
|---|---|---|
| `[출처]` | 인용 마커 | `[1]` |
| `[시점]` | 타임스탬프 | `03:42` |
| `[출처+시점]` | 인용 마커 + 타임스탬프 | `[1] 03:42` |

**모든** 주장·인용·항목 끝에 해당 토큰을 빠짐없이 채워라. 토큰을 빈 채로 두지 마라.

## 3. 환경 특화 제약 (NotebookLM)

- **인용 강제**: NotebookLM의 인용 기능을 활용하여 모든 출처 토큰에 실제 원문 링크가 걸리는 인용 마커(`[1]`, `[2]` …)를 생성하라. 연구자의 기계적 검증 마찰을 최소화하기 위함이다.
- **선택 소스 한정**: 체크박스에서 '선택'된 해당 영상 소스 범위 내에서만 답하라. 선택되지 않은 다른 소스나 외부 지식을 끌어들이지 마라. (코어 제약 1의 NotebookLM 강제판)

## 4. 입력 전달

아래를 복사한 후 괄호 안을 채워 코어·어댑터 뒤에 이어 실행하세요. (모두 선택 입력 — 비워도 작동합니다. 모드는 deep로 고정이므로 입력하지 않습니다.)

- **(선택) 부연 설명**: [예: "뼈대 재구성은 5단락 이내로 묶어줘"; "게스트 발언 위주로 Core 논증을 두껍게"; "이 영상은 다른 영상에 대한 반응이야 — 원본 콘텐츠와 화자 입장을 분리해줘"]
- **(선택) 연구 맥락**: [예: "프롬프트 설계 영상 시리즈를 누적 정리 중"]
- **(선택) 특별 주목 지점**: [예: "특정 화자 발언만 별도 정리"]

---

버전: v1.1 (실행 채널을 Studio 보고서 기능으로 명시) | 상태: draft | 최종 수정: 2026-05-29
